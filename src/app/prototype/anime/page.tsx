// PROTOTYPE route — THROWAWAY. Anime detail + book recs (chosen design). /prototype/anime
import { AnimeDetailPage } from "./variants";

export default function Page() {
  return (
    <main className="min-h-screen bg-zinc-950 text-zinc-100">
      <AnimeDetailPage />
    </main>
  );
}
